# calendar-sheets-appscript-etl
Google Apps Script tool that pulls interview/lead calls from Google Calendar into a Google Sheet for a chosen date range — parses round, discipline, BD, and interview type automatically.
